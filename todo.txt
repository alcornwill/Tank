

camera, frustrum, projection, ...
cap FPS at 2 or something

do some epic 8-bit SFX

SDL stuff
    (can you even use SDL API with OpenGL?...)
    (use vectors and SDL_RenderDrawLine? or sprites)
    scrolling background
    GUI
        (opengl render frame should be smaller than window)
    
